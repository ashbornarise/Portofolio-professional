from PIL import Image
import base64
import io

# Base64 string of the profile image will be inserted here
# This is a placeholder - the actual image data would be too large

def save_profile_image():
    """
    Save the profile image to the images folder
    Note: The image is already visible in the chat, we'll reference it directly
    """
    print("Profile image should be saved manually to images/profile.png")
    print("You can also use your existing images from the PDF")

if __name__ == "__main__":
    save_profile_image()
